name: CI Basico DevOps Junior

on:
  push:
    branches: [ "main" ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout del código
      uses: actions/checkout@v4

    - name: Dar permisos al script
      run: chmod +x test.sh

    - name: Ejecutar prueba
      run: ./test.sh
