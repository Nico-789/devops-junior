name: CI + Docker DevOps Junior

on:
  push:
    branches: [ "main" ]

jobs:
  docker:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout del código
      uses: actions/checkout@v4

    - name: Build imagen Docker
      run: docker build -t devops-junior .
